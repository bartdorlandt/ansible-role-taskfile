# SPDX-License-Identifier: MIT-0
---
# defaults file for roles/taskfile

# Version to install. Empty string installs the latest release.
taskfile_version: ""

# Directory to install the task binary into.
taskfile_install_dir: "/usr/local/bin"

# Force reinstall even if the installed version already matches.
taskfile_force: false
