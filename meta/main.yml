# SPDX-License-Identifier: MIT-0
galaxy_info:
  author: bartdorlandt
  role_name: taskfile
  description: Install the Task CLI tool (taskfile.dev) using the official install script
  license: MIT
  issue_tracker_url: https://github.com/bartdorlandt/ansible-role-taskfile/issues

  min_ansible_version: "2.9"

  galaxy_tags:
    - task
    - taskfile
    - cli

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
